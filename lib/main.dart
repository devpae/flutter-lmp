import 'package:flutter/material.dart';

import 'home.dart';

import 'product.dart';

void main() {
  runApp(
      MaterialApp(
        title: 'Flutter Demo',
        debugShowCheckedModeBanner: false,
        home: HomePage(),
      )
  );
}